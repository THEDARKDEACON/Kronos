# =============================================================================
# Kronos — full install (pipeline + dashboard + backtest + tests)
#   pip install -r requirements.txt
#
# NOT included here (install separately when needed):
#   webui/          → pip install -r webui/requirements.txt
#   finetune/       → pip install pyqlib comet_ml
#   finetune_csv/   → pip install pyyaml
#   FIX broker      → quickfix (system build deps; optional)
# =============================================================================

# --- Core data & numerics ---
numpy
pandas
scipy
pyarrow
lxml
requests>=2.31.0
yfinance

# --- Kronos model (Hugging Face) ---
torch>=2.0.0
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.6.2
tqdm==4.67.1
matplotlib==3.9.3

# --- Portfolio optimization ---
cvxpy>=1.3.0

# --- Ensemble: FinBERT / sentiment (optional at runtime if no NEWSAPI_KEY) ---
transformers>=4.30.0
accelerate>=0.20.0
onnx>=1.14.0
onnxruntime>=1.16.0

# --- Async data utilities (data/async_ingestion.py) ---
aiohttp>=3.8.0

# --- Broker APIs (paper/live; skipped in KRONOS_MODE=dry) ---
alpaca-trade-api>=3.0.0
ib-insync>=0.9.70

# --- Macro data (optional; FRED_API_KEY) ---
fredapi>=0.5.0

# --- Research / MLOps (research/experiment_tracker.py) ---
mlflow>=2.8.0
scikit-learn>=1.3.0

# --- Dashboard (streamlit run dashboard/app.py) ---
streamlit>=1.28.0
plotly>=5.18.0
streamlit-autorefresh>=0.0.1

# --- Production runner & ops scripts ---
schedule>=1.2.0
python-dotenv>=1.0.0
psutil>=5.9.0

# --- Testing ---
pytest>=7.0.0

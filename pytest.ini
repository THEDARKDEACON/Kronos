[pytest]
markers =
    integration: tests that download models or hit external APIs (set RUN_MODEL_TESTS=1)
    slow: long-running tests (backtest, full pipeline)

filterwarnings =
    ignore::DeprecationWarning:pandas.*
